# 薅秃奥特曼项目文件结构说明

本文档描述当前仓库中对现有产品能力仍然有意义的结构。

当前产品边界已经收敛为：

- `CPA API`
- `待注册邮箱列表 / 已注册邮箱列表`
- `Gmail IMAP + App Password`
- `Chrome Native Messaging`
- `auto-run`

## 根目录

- `manifest.json`
  Chrome 扩展清单。当前主链围绕 OpenAI / ChatGPT 页面、background service worker 和 nativeMessaging 权限工作。
- `background.js`
  后台 Service Worker 入口；负责模块装配、流程编排、状态管理与 Chrome 事件接线。
- `native_host/`
  本地 Chrome Native Messaging 宿主进程。
- `sidepanel/`
  当前侧边栏配置与运行状态界面。
- `tests/`
  Node / Python 回归测试。

### 仍保留但不属于当前主链能力的历史文件

以下文件仍可能留在仓库中，但不应视为当前成功路径入口：

- `content/gmail-mail.js`
- `content/vps-panel.js`
- `hotmail-utils.js`
- `luckmail-utils.js`
- `icloud-utils.js`
- `cloudflare-temp-email-utils.js`
- `managed-alias-utils.js`
- `microsoft-email.js`

## `background/`

- `background/native-host.js`
  扩展到 native host 的调用桥。
- `background/oauth-runtime.js`
  本地 OAuth PKCE 运行时生成器。
- `background/cpa-auth-upload.js`
  Step 10 的后台 token exchange / auth-file upload 协调层。
- `background/custom-email-pool.js`
  待注册邮箱 / 已注册邮箱列表规范化与迁移逻辑。
- `background/message-router.js`
  后台消息路由。
- `background/verification-flow.js`
  Gmail IMAP 验证码提交、重发与错误恢复控制。
- `background/tab-runtime.js`
  标签页与内容脚本运行时基础设施。
- `background/navigation-utils.js`
  URL 与 OAuth callback 判断辅助。
- `background/auto-run-controller.js`
  自动运行控制器。

### `background/steps/`

- `open-chatgpt.js`
  Step 1，打开 ChatGPT 并立即触发 Cookie 清理。
- `submit-signup-email.js`
  Step 2，提交注册邮箱。
- `fill-password.js`
  Step 3，填写密码。
- `fetch-signup-code.js`
  Step 4，获取注册验证码。
- `fill-profile.js`
  Step 5，填写姓名与生日。
- `clear-login-cookies.js`
  Step 6，兼容占位步骤。
- `oauth-login.js`
  Step 7，生成本地 OAuth URL 并执行登录。
- `fetch-login-code.js`
  Step 8，获取登录验证码。
- `confirm-oauth.js`
  Step 9，自动确认 OAuth 并捕获 localhost callback。
- `platform-verify.js`
  Step 10，后台上传 CPA 认证文件。

## `native_host/`

- `host.py`
  Native Messaging 主入口。
- `messages.py`
  Chrome Native Messaging 长度前缀消息协议。
- `gmail_imap.py`
  Gmail IMAP 登录、邮件抓取、验证码提取。
- `openai_oauth.py`
  localhost callback 解析与 OpenAI token 交换。
- `cpa_upload.py`
  CPA auth-file payload 生成与上传。
- `install_host_manifest.py`
  安装本地 host manifest 的辅助脚本。
- `README.md`
  本地 host 的安装和命令约定说明。

## `sidepanel/`

- `sidepanel/sidepanel.html`
  当前侧边栏结构；包含 CPA API、Gmail IMAP、待注册邮箱列表、已注册邮箱列表、auto-run 配置入口。
- `sidepanel/sidepanel.js`
  当前侧边栏主逻辑。
- `sidepanel/custom-email-manager.js`
  待注册邮箱列表的视图管理与进度渲染。
- `sidepanel/registered-email-manager.js`
  已注册邮箱列表的视图管理、编辑和清空操作。
- `sidepanel/sidepanel.css`
  样式文件。

## `content/`

- `content/signup-page.js`
  OpenAI / ChatGPT 页面步骤执行逻辑。
- `content/auth-page-recovery.js`
  认证页重试与恢复逻辑。
- `content/utils.js`
  内容脚本公共工具。

说明：

- `content/gmail-mail.js` 与 `content/vps-panel.js` 不是当前主链入口。

## `data/`

- `data/step-definitions.js`
  当前 10 步定义。
- `data/names.js`
  随机姓名、生日等资料数据源。

## `tests/`

当前主链测试主要覆盖：

- native host bridge
- Gmail IMAP 配置与后台验证码流程
- 本地 OAuth runtime
- Step 10 后台 CPA 上传
- 待注册邮箱迁移到已注册邮箱
- sidepanel 的待注册 / 已注册邮箱列表管理

## 文档维护规则

- 当 Gmail IMAP、OAuth、CPA 上传、邮箱列表模型发生变化时，必须同步更新本文档。
- 当继续删除历史 provider 遗留代码后，应同步缩短“历史文件”列表。
