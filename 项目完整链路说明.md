# 薅秃奥特曼完整链路说明

本文档描述当前仍然保留的主链路，不再把旧版 Hotmail / LuckMail / iCloud / SUB2API / 浏览器邮箱页轮询 / 网页面板回填当作产品能力。

当前产品已经收敛为：

- `CPA API + 管理密钥`
- `待注册邮箱列表 / 已注册邮箱列表`
- `Gmail IMAP + App Password`
- `Chrome Native Messaging`
- `auto-run`

## 1. 当前目标

这是一个 Chrome 扩展，用于自动执行一套 ChatGPT / OpenAI 注册与 OAuth 授权流程。

当前最小可用链路是：

1. 打开 ChatGPT 官网后立即清理登录 Cookies
2. 使用待注册邮箱完成注册与登录验证码流程
3. 通过 Gmail IMAP 在后台获取验证码，不再依赖浏览器邮箱页轮询
4. 在本地生成 OAuth PKCE 运行时并完成授权
5. 在后台交换 localhost 回调并上传 CPA 认证文件
6. 成功同步后把邮箱从待注册列表迁移到已注册列表

## 2. 当前核心参与者

### 2.1 Sidepanel

文件：

- `sidepanel/sidepanel.html`
- `sidepanel/sidepanel.js`
- `sidepanel/custom-email-manager.js`
- `sidepanel/registered-email-manager.js`

职责：

- 接收 `CPA API` / 管理密钥配置
- 接收 `Gmail IMAP` 配置
- 管理待注册邮箱列表与已注册邮箱列表
- 展示步骤状态、日志和运行状态
- 触发单步执行、跳步、停止、重置
- 配置和控制 `auto-run`

### 2.2 Background Service Worker

文件：

- `background.js`
- `background/message-router.js`
- `background/verification-flow.js`
- `background/oauth-runtime.js`
- `background/cpa-auth-upload.js`
- `background/custom-email-pool.js`
- `background/tab-runtime.js`

职责：

- 统一接收 sidepanel 与内容脚本消息
- 管理运行态与持久化配置
- 编排 10 步执行
- 通过 native host 获取 Gmail 验证码
- 生成本地 OAuth PKCE 运行时
- 触发 OAuth 回调交换与 CPA auth-file 上传

### 2.3 Native Host

文件：

- `native_host/host.py`
- `native_host/gmail_imap.py`
- `native_host/openai_oauth.py`
- `native_host/cpa_upload.py`
- `native_host/messages.py`

职责：

- Gmail IMAP 登录与验证码轮询
- OpenAI OAuth callback code 换 token
- 生成 CPA 兼容 auth JSON
- 上传 auth file 到 `CPA /v0/management/auth-files`

### 2.4 Content Scripts

文件：

- `content/signup-page.js`
- `content/auth-page-recovery.js`
- `content/utils.js`

职责：

- 在 ChatGPT / OpenAI 认证页面执行 DOM 交互
- 处理注册、密码、验证码提交、授权确认等页面动作

说明：

- `content/gmail-mail.js` 和 `content/vps-panel.js` 仍可能暂时保留在仓库中，但它们已经不属于主链成功路径。

## 3. 配置与状态模型

### 3.1 当前重要持久化配置

- `cpaApiUrl`
- `cpaManagementKey`
- `gmailImapEmail`
- `gmailImapAppPassword`
- `gmailImapHost`
- `gmailImapPort`
- `customPassword`
- `customEmailList`
- `customEmailUsedMap`
- `registeredEmailList`
- `autoRunSkipFailures`
- `autoRunFallbackThreadIntervalMinutes`
- `autoRunDelayEnabled`
- `autoRunDelayMinutes`
- `autoStepDelaySeconds`
- `verificationResendCount`

### 3.2 当前重要运行态

- `email`
- `password`
- `oauthUrl`
- `oauthRuntime`
- `localhostUrl`
- `lastSignupCode`
- `lastLoginCode`
- `signupVerificationRequestedAt`
- `loginVerificationRequestedAt`
- `autoRunPhase`
- `autoRunCurrentRun`
- `autoRunTotalRuns`
- `autoRunAttemptRun`

## 4. 邮箱列表模型

### 4.1 待注册邮箱列表

- 来源字段：`customEmailList`
- 进度字段：`customEmailUsedMap`
- 后台按列表顺序分配
- 同一轮运行中优先复用当前邮箱

### 4.2 已注册邮箱列表

- 来源字段：`registeredEmailList`
- 支持手动编辑、粘贴导入、清空
- Step 10 上传成功后自动追加

### 4.3 迁移规则

只有满足下面两个条件时，邮箱才会从待注册列表迁移到已注册列表：

1. 注册流程已完成
2. Step 10 的 CPA auth-file 上传成功

如果 Step 10 失败：

- 不迁移到 `registeredEmailList`
- 不从 `customEmailList` 删除
- 保留重试能力

## 5. Gmail IMAP 验证码链路

### Step 4

1. 注册页请求验证码
2. background 调用 native host `gmail.waitForVerificationCode`
3. native host 通过 Gmail IMAP 轮询验证码邮件
4. background 把验证码提交到注册页

### Step 8

1. 登录页请求验证码
2. background 再次调用 native host `gmail.waitForVerificationCode`
3. native host 返回最新匹配验证码
4. background 提交到登录验证页

说明：

- 主链不再打开 Gmail 收件页
- 使用 `Gmail IMAP + App Password`

## 6. OAuth 与 CPA 链路

### Step 7

1. background 在本地生成 `state`、`codeVerifier`
2. 计算 PKCE challenge
3. 生成新的 OpenAI OAuth URL
4. 保存 `oauthRuntime`
5. 打开本地生成的 OAuth URL 并继续登录

### Step 9

1. 在 OAuth 同意页自动点击继续
2. 监听 localhost 回调
3. 把 `localhostUrl` 写入运行态

### Step 10

1. background 校验 `localhostUrl` 与 `oauthRuntime`
2. 调用 native host `oauth.exchangeCallback`
3. native host 向 OpenAI token endpoint 换取 token
4. background 调用 native host `cpa.uploadAuthFile`
5. native host 生成 auth JSON 并上传到 CPA
6. 上传成功后完成 Step 10，并触发邮箱迁移

说明：

- Step 10 不再打开 CPA 页面
- Step 10 不再通过页面提交 callback URL

## 7. 当前 10 步主流程

### Step 1

打开 ChatGPT 官网，并在页面就绪后立即清理 ChatGPT / OpenAI 登录 Cookies。

### Step 2

点击注册入口，提交邮箱，并判断进入密码页还是验证码页。

### Step 3

生成或读取密码，填写并继续。

### Step 4

通过 Gmail IMAP 在后台获取注册验证码并提交。

### Step 5

填写姓名与生日。

### Step 6

兼容占位步骤；不再执行延迟 Cookie 清理。

### Step 7

本地生成 OAuth URL 并进入登录流程。

### Step 8

通过 Gmail IMAP 在后台获取登录验证码并提交。

### Step 9

在 OAuth 同意页自动点击继续，并监听 localhost 回调。

### Step 10

在后台交换 OAuth callback 并上传 CPA 认证文件。

## 8. auto-run

`auto-run` 当前围绕待注册邮箱列表工作。

行为：

- 如果当前已有邮箱，则继续当前轮
- 如果当前邮箱为空但待注册列表仍有剩余，则自动分配下一封
- 如果待注册列表耗尽，则暂停等待人工处理
- 一轮成功后清空当前运行态邮箱
- 成功同步的邮箱自动归档到已注册列表

## 9. 当前不再属于主链的能力

以下能力不再视为当前产品功能描述：

- 浏览器邮箱页轮询
- 通过网页面板获取 OAuth 链接
- 通过网页面板提交 callback
- SUB2API
- Hotmail
- LuckMail
- iCloud
- Duck / Cloudflare / Cloudflare Temp Email 自动生成邮箱

## 10. 维护原则

后续继续清理仓库时，应遵守这条原则：

> 任何删除都不能破坏 `CPA API + 待注册/已注册邮箱列表 + Gmail IMAP + native host + auto-run` 主链。
